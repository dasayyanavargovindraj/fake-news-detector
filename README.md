# fake-news-detector
Fake News Detector is an AI-powered tool that analyzes news content and helps identify whether information is likely to be genuine or misleading.
Fake News Detector is an AI-powered tool that analyzes news content and helps identify whether information is likely to be genuine or misleading.simple and interactive interface.
inside terminal write python -m venv venv
next type venv\scripts\activate
# Liberries 
pip install streamlit 
pip install langchain
pip install langchain-community
pip install langchain-google-genai
pip install google-generativeai
pip install faiss-cpu
pip install pypdf
pip install python-docx
pip install senetence-transformers
pip install python-dotenv
